// _ <-- naming
// -----------------------------------------
// don't name things starting with _ 
//     (_data, X)


// don't use double __
//      (my__data, X)


// -----------------------------------------

// macros
//      try to not use macro at all, which is replacable with const, inline, typedef, etc
//      textEditor textEditor
//      textEditor.SetTextSelectionPos(0, 3); // too long
//      textEditor.RemoveTextSelection();

//      TextEditor textEditor;
//      textEditor.Select(0, 3);             // way better
//      textEditor.Remove();

// "Good coing STARTS with good naming!"
